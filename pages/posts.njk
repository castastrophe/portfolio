---
layout: base
masthead: true
eleventyNavigation:
  key: Writing
  order: 3
---

<a-band id="post-listing" thick>
    <h2 class="title" slot="header">Posts</h2>
    {% for article in collections.posts | reverse %}
    {% if article.data.title %}
        <a-card bordered>
            <div slot="header">
                <p class="date">{{ article.data.date | long }}</p>
                <h3 class="headline accent"><a href="{{ article.url }}">{{ article.data.title }}</a></h3>
            </div>
            <p class="content">{{ article.data.description }}</p>
            {% if article.data.tags.length > 0 %}<tag-group tags="{{ article.data.tags | join(',') }}" labeled slot="footer"></tag-group>{% endif %}
        </a-card>
    {% endif %}
    {% endfor %}
    <a-note style="align-self: center" slot="footer"></a-note>
    <img slot="footer" src="../img/cartoon-thinking.png" alt="A cartoon of the author, Cassondra Roberts, thinking about the future of the web.">
</a-band>

{% css %}
.date {
    font-weight: var(--theme--FontWeight--light);
}
.headline {
    margin-block-end: var(--theme--content--space);
}
{% endcss %}
